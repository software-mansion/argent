{
  "name": "radon-lite-workspace",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w packages/registry && npm run build -w packages/tools && npm run build -w packages/mcp"
  }
}
