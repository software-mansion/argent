{
  "name": "argent-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "start": "npm run build -w @argent/registry && npm run dev -w @argent/tool-server",
    "start:tool-server": "npm run build -w @argent/registry && npm run dev -w @argent/tool-server",
    "download:simulator-server": "node scripts/download-simulator-server.cjs",
    "download:native-binaries": "node scripts/download-native-binaries.cjs",
    "pack": "npm run download:simulator-server && npm run download:native-binaries && npm run build && npm run build -w @swmansion/argent && npm pack -w @swmansion/argent --pack-destination .",
    "pack:mcp": "npm run pack",
    "dev": "node scripts/dev.cjs",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
