// Empty on purpose: this component is registered only while DocSearch is off
// (see ../../index.js), and no search UI is rendered in that state. The one
// reader — the search theme's Ask AI hook — reads every field optionally.
export default {};
