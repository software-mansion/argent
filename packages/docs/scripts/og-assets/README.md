# Open Graph assets

`DMSans-Medium.ttf` is the DM Sans 500 static instance from Google Fonts. The build time
Open Graph card generator in `plugins/og-image/` embeds it, so the cards render the same
on every machine and in CI, with no system font installed.

The font is licensed under the SIL Open Font License 1.1. See `OFL.txt`.
