# Link the static musl binary with the toolchain-bundled rust-lld so no external
# aarch64 cross-gcc is required on the build host (macOS).
[target.aarch64-unknown-linux-musl]
linker = "rust-lld"
rustflags = ["-C", "target-feature=+crt-static"]
